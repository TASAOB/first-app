import Alert from "./components/Alert";

function App() {
  // let items = ["A", "B", "C", "D", "E"];

  // const handleSelectItem = (item: string) => {
  //   console.log(item);
  // };

  return (
    <div>
      {/* <ListGroup
        items={items}
        heading={"Letters"}
        onSelectItem={handleSelectItem}
      /> */}
      <Alert />
    </div>
  );
}

export default App;
